specVersion: "v1"
serviceId: "tradeupapp"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "tradeupapp-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
